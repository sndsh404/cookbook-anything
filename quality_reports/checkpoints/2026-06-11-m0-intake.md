# Checkpoint: M0 intake + trace + secret filter

Date: 2026-06-11 (session 1)

## The number

12 planted secrets -> 0 leaks into spans. Also: 3 source types (git repo,
PDF, mixed folder) parse with per-source trace timelines; rerun on unchanged
sources reparses 0 (sha256 manifest check).

## What shipped

- `scripts/intake.py`: source router (git_repo/folder/pdf/markdown/csv/sql),
  secret filter (6 pattern kinds + Shannon-entropy scan), file-level span
  store with redacted text, manifest, per-source trace jsonl, runs.jsonl.
- Minimal stdlib PDF text-layer extractor (FlateDecode + Tj/TJ); honest
  trace fallback when there is no text layer.
- `tests/test_m0_intake.py`: builds the fixture (including a real PDF with a
  planted secret in its text stream), asserts all three numbers.

## Decisions

- Span store holds REDACTED text under .cookbook/ and is canonical for all
  downstream stages; locators still point at the originals. This is what
  makes "0 leaks into spans" enforceable.
- PDF support is a minimal stdlib extractor, not a dependency. Real-world
  PDFs without simple text streams fall back with a trace note, per DESIGN
  honest seam #4.

## Result

test_m0_intake.py exit 0. METRIC secrets_leaked=0, m0_sources_parsed=3,
m0_rerun_reparsed=0.
