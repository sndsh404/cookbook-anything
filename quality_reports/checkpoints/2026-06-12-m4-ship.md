# Checkpoint: M4 ship a real paper

Date: 2026-06-12 (session 1)

## The numbers

Full pipeline (stages.ts: intake -> compile -> topology -> plan -> write ->
verify -> figures -> lint -> grade) on llmwiki ships at **grade 99/100**
(gate >= 80), zero P0, page-one figure present, all 4 chapters figured,
claims + unverified appendices emitted. First run scored 90; the human
review filed 5 issues; fixing 4 of them raised it to 99.

## What shipped

- `runner/stages.ts` the stage runner: invokes the Rust core and the Python
  renderer in order, relays the scored gate's verdict.
- `figlib/figures_from_plan.py` plan + model -> typed payloads -> rendered
  figures -> figcheck report for the grader.
- `tests/test_m4_ship.py` the end-to-end gate.
- `quality_reports/issues/2026-06-12-paper-review.md` 5 filed issues; 4
  fixed (redaction artifacts in claims, boilerplate captions, undocumented
  files dominating bullets, grader/linter scope mismatch), 1 recorded
  (thin-chapter folding).

## The feedback loop worked as designed

Review issue 1 (a "[REDACTED:high_entropy]" artifact quoted into the intro)
became both an extractor rule (claim_worthy rejects mutilated sentences)
and a new named lint (prose-redaction-artifact, P1). That is DESIGN v2
("every real complaint becomes a named, checkable rule") happening in v0.

## Result

test_m4_ship.py exit 0. METRIC m4_grade=99, m4_chapters_figured=4,
m4_figure_p0=0. assess.py: 99/100 green, all milestone gates passing.
